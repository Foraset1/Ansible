---
- hosts: all
  become: yes
  tasks:
    - name: Install required packages
      apt:
        name:
          - cron
          - nano
          - redsocks
          - iptables-persistent
        state: present
        update_cache: yes
